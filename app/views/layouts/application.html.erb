<!DOCTYPE html>
<html>

<head>
  <title>Harshvardhan Parihar | Welcome</title>
  <%= render "common/css" %>
  <%= csrf_meta_tags %>
  <%= display_meta_tags :site => 'Harshvardhan Parihar', :reverse => true %>
</head>

<body class="header-fixed">
  <%= yield :after_body %>
  <div class="wrapper">
    <%= render "common/header" -%>
    <%= yield %>
    <%= render "common/footer" -%>
  </div>
    <div id="popup"></div>
    <%= render "common/js" %>
</body>

</html>
